describe('Cypress Studio Professional Profile Tests', () => {
  it('records a new functional test', () => {
    // We start with a visit so the Studio has a page to work with
    cy.visit('/');
  });
});